<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eud.sm.app.repository.SearchRepository">

    <select id="findByAddtAndType" parameterType="Integer" resultType="Search">
        SELECT * FROM search where
    </select>
    <select id="selectAll" resultType="Search">
        SELECT * FROM search
    </select>
    <insert id="insert" parameterType="Search">
        INSERT INTO search VALUES (#{type}, #{name})
    </insert>
    <update id="update" parameterType="Search">
        UPDATE search SET type=#{type}, name=#{name} where type=#{type};
    </update>
    <delete id="delete" parameterType="Search">
        DELETE FROM search WHERE type=#{type}
    </delete>

</mapper>